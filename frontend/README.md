# Frontend Dashboard

This folder contains a React frontend for the sales analytics portfolio.

## Run

```bash
cd frontend
npm install
npm run dev
```

The `sync:data` step copies the current CSV outputs from `../output` into `frontend/public/data` before the app starts.
